Offline local version

This version does NOT use Firebase.
All data is saved inside the browser using localStorage.

Important:
- Data stays on the same browser/device only.
- If you open the site from another device, the data will not appear there.
- Clearing browser storage will erase data.
- Admin can reset the system from Settings > Reset System.

Default passwords after reset:
- All users: 111111